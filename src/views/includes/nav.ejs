<nav class="navbar navbar-expand-lg bg-body-tertiary navbar-dark m-0">
    <!--container-->
    <div class="container-fluid  bg-dark">
        <!--Opções-->
        <a class="navbar-brand" href="/">Agenda</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto" />
            <li class="nav-item">
                <a class="nav-link" href="/contatos/index">Cadastrar contatos </a>
            </li>
            <!--E se o usuario existe, exibe opção de fazer logout-->
            <% if(user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/login/logout">Sair</a>
                </li>
                <!--E se o usuario não existe, exibe opção de fazer login-->
                <% } else {%>
                    <li class="nav-item">
                        <a class="nav-link" href="/login/index">Entrar </a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>