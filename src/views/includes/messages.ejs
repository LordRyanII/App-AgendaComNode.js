<!--ALERTA-->

<!--Erro-->
<% if(errors.length > 0) { %>
    <div class="row">
        <div class="col my-3">
            <div class="alert alert-danger">
                <% errors.forEach(erro=> { %>
                    <%= erro %> <br>
                        <% }); %>
            </div>
        </div>
    </div>

<% }; %>

<!--Sucesso-->
<% if(sucess.length > 0) { %>
    <div class="row">
        <div class="col my-3">
            <div class="alert alert-sucess">
                <% errors.forEach(sucess=> { %>
                    <%= erro %> <br>
                        <% }); %>
            </div>
        </div>
    </div>

<% }; %>